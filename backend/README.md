# CodeOptAI Backend

## Run

1. Copy `.env.example` to `.env`
2. Add your API key to `GROQ_API_KEY`
3. Keep `ALLOW_FALLBACK=false` to force real AI optimization
4. Start server:

```bash
npm install
npm run dev
```
